<settings>
  <profiles>
    <profile>
      <id>anypoint</id>
      <properties>
        <anypoint.username>${{ secrets.ANYPOINT_USERNAME }}</anypoint.username>
        <anypoint.password>${{ secrets.ANYPOINT_PASSWORD }}</anypoint.password>
        <anypoint.org>${{ secrets.ANYPOINT_ORG }}</anypoint.org>
        <anypoint.environment>${{ secrets.ANYPOINT_ENV }}</anypoint.environment>
      </properties>
    </profile>
  </profiles>

  <activeProfiles>
    <activeProfile>anypoint</activeProfile>
  </activeProfiles>
</settings>
